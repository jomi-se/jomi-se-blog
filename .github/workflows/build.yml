name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Build (zero-dependency aggregator)
        run: node build.js
      - name: Sanity-check output
        run: test -f dist/index.html && test -f dist/feed.xml && test -f dist/sitemap.xml
