<!-- This pointer exists because Claude Code auto-loads CLAUDE.md, not AGENTS.md.
     The canonical guidance lives in AGENTS.md so Codex and other agents read the
     same file. The @import below pulls it into Claude Code's context at launch —
     one source of truth, no duplication. Add Claude-specific notes below the import
     if ever needed. (This comment is stripped before entering context.) -->

@AGENTS.md
